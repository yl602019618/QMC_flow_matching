# M1 与已有 RQMC 估计量的数学对比

## 0. 共同的目标

4-component GMM 目标分布

$$
\pi(x) = \frac{1}{K}\sum_{k=1}^{K}\mathcal{N}(x \mid \mu_k, \Sigma_k),
\qquad K = 4,\ d = 2\ \text{或}\ 30.
$$

写成层级形式：

$$
K_i \sim \mathrm{Uniform}\{1,\dots,K\},
\quad
Z_i \sim \mathcal{N}(0, I_d),
\quad
X_i = \mu_{K_i} + L_{K_i} Z_i,
\quad
L_k L_k^\top = \Sigma_k.
$$

对一族被积函数 $\mathcal{F} = \{x \mapsto x_j,\ x \mapsto x_j^2,\ f_{\text{complex}}(x)\}$，所有估计量都取

$$
\hat I_N(f) = \frac{1}{N}\sum_{i=1}^{N} f(X_i),\qquad f\in\mathcal{F},
$$

不同方法只在 $\{(K_i, Z_i)\}_{i=1}^N$ 的产生方式上不同。RMSE 为

$$
\mathrm{RMSE}_N(f) = \left( \mathbb{E}\bigl[\bigl(\hat I_N(f) - \mathbb{E}_\pi f\bigr)^2\bigr]\right)^{1/2}.
$$

按 $\log\mathrm{RMSE} = s\,\log N + c$ 线性拟合斜率 $s$。

---

## 1. 三种构造方式的数学定义

### 1.1 Baseline A — MC bucket + RQMC Gauss

抽样定义：

$$
K_i \overset{\text{iid}}{\sim} \mathrm{Uniform}\{1,\dots,K\},
\qquad
\{u^B_i\}_{i=1}^N\subset (0,1)^d\ \text{scrambled Sobol'},
\qquad
Z_i = \Phi^{-1}(u^B_i).
$$

桶分配是离散均匀 i.i.d.；Gaussian 走逆 CDF。两部分通过 same index $i$ 配对。

### 1.2 Baseline B — RQMC bucket + RQMC Gauss（两条独立 scrambled 流）

$$
\{u^A_i\}_{i=1}^N \subset (0,1)^1\ \text{scrambled Sobol'},
\qquad
K_i = \lfloor K\,u^A_i\rfloor + 1.
$$

$$
\{u^B_i\}_{i=1}^N \subset (0,1)^d\ \text{scrambled Sobol' (独立 scramble)},
\qquad
Z_i = \Phi^{-1}(u^B_i).
$$

两条 Sobol' 流的 Owen scramble 独立，但 index $i$ 共享 base-2 radical-inverse 位结构。

### 1.3 M1 — 联合 (d+1) 维 scrambled Sobol'

定义映射 $\phi : (0,1)^{d+1} \to \mathbb{R}^d$：

$$
\phi(u_0, u_1, \dots, u_d) = \mu_{\lfloor K u_0 \rfloor + 1} + L_{\lfloor K u_0\rfloor + 1}\,\Phi^{-1}(u_1, \dots, u_d).
$$

引理（变量替换）：若 $U \sim \mathrm{Uniform}\!\left((0,1)^{d+1}\right)$，则 $\phi(U) \sim \pi$。

证明思路：$\lfloor K u_0\rfloor + 1$ 在 $u_0$ 均匀时是 Uniform$\{1,\dots,K\}$；$\Phi^{-1}(u_{1:d})$ 在 $u_{1:d}$ 均匀时是 $\mathcal{N}(0, I_d)$；两者独立。$\square$

因此

$$
\mathbb{E}_\pi[f] = \int_{(0,1)^{d+1}} f\bigl(\phi(u)\bigr)\,du.
$$

M1 估计量：取 $\{u_i\}_{i=1}^N \subset (0,1)^{d+1}$ 为**一份**scrambled Sobol' 点列，

$$
\hat I_N^{\text{M1}}(f) = \frac{1}{N}\sum_{i=1}^{N} f\bigl(\phi(u_i)\bigr).
$$

代码层面：

$$
(K_i, Z_i) = \bigl(\lfloor K u_{i,0}\rfloor + 1,\ \Phi^{-1}(u_{i,1:d})\bigr),
$$

其中 $u_i$ 的所有 $d+1$ 个分量来自同一个 Sobol' 点。

---

## 2. 数学结构对比

### 2.1 联合点列的差异

记 $\mathcal P^{A}_N = \{(K_i, u^B_i)\}_{i=1}^N \subset \{1,\dots,K\}\times(0,1)^d$ 等。

| 方法 | 一对 $(K_i, Z_i)$ 来自 | 联合分布的 discrepancy |
| --- | --- | --- |
| Baseline A | $K_i \perp\!\!\!\perp u^B_i$，$K_i$ 是 MC，$u^B_i$ 是 RQMC | $K$ 维 MC 分量主导 |
| Baseline B | $u^A_i, u^B_i$ 独立 scramble，但**共享 index $i$ 的位结构** | 仅边缘 1-D 和 d-D 低差异，联合 $(d+1)$-D 一般不低差异 |
| **M1** | $u_i \in (0,1)^{d+1}$ 是同一份 Sobol' 的点 | **联合 $(d+1)$-D 低差异**（Owen-scrambled $(t, m, d+1)$-net） |

对 Baseline B 的"联合不低差异"，可形式化为：

$$
D_N^*\bigl(\{(u^A_i, u^B_i)\}\bigr) \ne O\!\bigl((\log N)^{d+1}/N\bigr),
$$

而 M1 满足

$$
\mathbb{E}\!\left[D_N^*\bigl(\{u_i\}\bigr)\right] = O\!\bigl((\log N)^{d+1}/N\bigr).
$$

### 2.2 二阶矩的方差分解

$$
\mathbb{E}_\pi[X_j^2]
=\underbrace{\mathbb{E}[\mu_{K,j}^2]}_{T_1}
+ \underbrace{2\,\mathbb{E}[\mu_{K,j}\,(L_K Z)_j]}_{T_2 \text{ (cross)}}
+ \underbrace{\mathbb{E}[(L_K Z)_j^2]}_{T_3}.
$$

写每个估计量分量误差的方差衰减率：

| 项 | Baseline A | Baseline B | M1 |
| --- | --- | --- | --- |
| $\widehat T_1$ | $\mathrm{Var} = O(N^{-1})$（MC 桶） | $\mathrm{Var} = O(N^{-2})$（auto-stratify） | $\mathrm{Var} = O(N^{-2})$ |
| $\widehat T_3$ | $\mathrm{Var} = O(N^{-2})$（d-D RQMC） | $\mathrm{Var} = O(N^{-2})$ | $\mathrm{Var} = O(N^{-2})$ |
| $\widehat T_2$（**cross**） | $\mathrm{Var} = O(N^{-1})$（MC × RQMC = MC） | $\mathrm{Var} = O(1)$**（plateau）** | $\mathrm{Var} = O(N^{-2})$ |

$T_2$ 是**联合积分**

$$
T_2 = 2\,\int_{(0,1)^{d+1}}
\mu_{\lfloor K u_0\rfloor + 1,j}\,
\bigl(L_{\lfloor K u_0\rfloor + 1}\Phi^{-1}(u_{1:d})\bigr)_j\,du,
$$

被积函数是 $u$ 的有界变差函数（在 $u_0\in\{1/K,\dots,(K-1)/K\}$ 处分段，每段内光滑）。

- Baseline B 用两条边缘流估它 → 不是 $(0,1)^{d+1}$ 上的 RQMC，方差不衰减。
- M1 是 $(0,1)^{d+1}$ 上的标准 RQMC，且分段边界 $u_0 = j/K$ 在 $K\mid N$ 时与 Sobol' base-2 节点对齐 → 方差按 $O(N^{-2}(\log N)^{2(d+1)})$ 衰减。

### 2.3 complex f 的关键展开

$$
f_{\text{complex}}(x) = -\Phi(1/\sqrt 2) + \Phi\!\left(1 + \frac{1}{\sqrt d}\mathbf 1^\top x\right).
$$

在 $X = \mu_K + L_K Z$ 下，

$$
\frac{\mathbf 1^\top X}{\sqrt d}
=\underbrace{\frac{\mathbf 1^\top \mu_K}{\sqrt d}}_{a_K}
+ \underbrace{\frac{\mathbf 1^\top L_K Z}{\sqrt d}}_{b_K Z'},\ \ Z'\sim\mathcal N(0,1).
$$

误差中同样含 $a_K \cdot Z'$ 形式的 cross term。结论与二阶矩一致：

| | $\widehat{f_{\text{complex}}}$ 方差衰减 |
| --- | --- |
| Baseline A | $O(N^{-1})$ |
| Baseline B | $O(1)$ |
| **M1** | $O(N^{-2})$（除非 $\mathbf 1/\sqrt d$ 方向恰好与 Sobol' 高 index 维度对齐，见 §3） |

---

## 3. 实测斜率对比

| 被积 | Baseline A | Baseline B | **M1** |
| --- | --- | --- | --- |
| 2D first moment | $-0.53$ | $-1.14$ | $-1.14$ |
| 2D second moment | $-0.46$ | $-0.08$ | **$-1.13$** |
| 2D complex f | $-0.59$ | $-0.13$ | **$-1.04$** |
| 30D first moment | $-0.55$ | $-1.11$ | $-1.10$ |
| 30D second moment | $-0.51$ | $-0.11$ | **$-1.02$** |
| 30D complex f | $-0.60$ | $-0.28$ | $-0.72$ |

理论与实测的一致性：

- **Baseline A**：所有被积都精确 $-1/2$ 阶（cross term + MC 桶限速）。
- **Baseline B**：一阶矩到 $-1$（cross term 不出现），二阶矩与 complex f 在 $0$ 阶 plateau（cross term 的联合不低差异）。
- **M1**：所有被积到 $-1$；唯一例外是 30D complex f 的 $-0.72$ — 它依赖单个 d 维方向 $\mathbf 1/\sqrt d$，被 Sobol' 高 index 维度的投影 discrepancy 拖累，与 $d+1$ 维联合低差异是一致的现象（高维 Sobol' 的常数因子退化），并不与 §2 的方差分析矛盾。

---

## 4. 总结公式

设 $\Pi_d$ 为 Sobol' base-2 $(d+1)$-net 在 $(0,1)^{d+1}$ 上的离散测度。三种方法的偏差—方差结构分别是：

$$
\hat I^{\text{A}}_N(f) - \mathbb{E}_\pi f
\;\stackrel{N\to\infty}{=}\;
O_p(N^{-1/2}).
$$

$$
\hat I^{\text{B}}_N(f) - \mathbb{E}_\pi f
\;\stackrel{N\to\infty}{=}\;
\underbrace{O_p(N^{-1})}_{\text{marginal-only terms}}
\;+\;
\underbrace{O_p(1)\cdot \mathbb{1}_{\{f \text{ 含 cross term}\}}}_{\text{joint coupling failure}}.
$$

$$
\hat I^{\text{M1}}_N(f) - \mathbb{E}_\pi f
\;\stackrel{N\to\infty}{=}\;
O_p\!\bigl(N^{-1}(\log N)^{d+1}\bigr).
$$

一句话：**Baseline B 把抽桶和抽 Gauss 当作两个独立的边缘 RQMC 子问题；M1 把它们当作同一个 $(d+1)$ 维 RQMC 子问题**。这一改动把 cross-term 上的 $O(1)$ plateau 变成 $O(N^{-1})$ 收敛。
